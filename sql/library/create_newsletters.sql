CREATE TABLE NEWSLETTERS (
 FILENAME				VARCHAR(64) NOT NULL,
 NAME					VARCHAR(96) NOT NULL,
 CATEGORY				VARCHAR(64) NOT NULL,
 FILESIZE				INTEGER NOT NULL,
 SECURITY				SMALLINT,
 PUBLUSHED				DATETIME NOT NULL,
 BODY					TEXT NOT NULL,
 PRIMARY KEY(FILENAME)
);