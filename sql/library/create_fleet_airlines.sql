CREATE TABLE FLEET_AIRLINE (
 FILENAME				VARCHAR(64) NOT NULL,
 CODE					VARCHAR(3) NOT NULL,
 PRIMARY KEY (FILENAME, CODE),
 FOREIGN KEY (FILENAME) REFERENCES FLEET(FILENAME) ON DELETE CASCADE ON UPDATE CASCADE,
 FOREIGN KEY (CODE) REFERENCES common.AIRLINEINFO(CODE) ON DELETE CASCADE ON UPDATE CASCADE
);