CREATE TABLE DOWNLOADS (
 FILENAME			VARCHAR(64) NOT NULL,
 DATE				DATETIME NOT NULL,
 USER_ID			INTEGER UNSIGNED NOT NULL,
 PRIMARY KEY (FILENAME, DATE),
 FOREIGN KEY (USER_ID) REFERENCES PILOTS(ID)
);
