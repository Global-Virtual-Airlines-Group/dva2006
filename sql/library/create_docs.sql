CREATE TABLE DOCS (
 FILENAME			VARCHAR(64) NOT NULL,
 NAME				VARCHAR(96) NOT NULL,
 FILESIZE			INTEGER NOT NULL,
 VERSION			SMALLINT NOT NULL,
 SECURITY			SMALLINT,
 ONREG				BOOLEAN DEFAULT 0,
 BODY				TEXT NOT NULL,
 PRIMARY KEY(FILENAME)
);